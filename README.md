Current State: The codebase resides on the Windows host. Scripts in the repo invoke wsl.exe to run commands inside the WSL environment. 
The development is managed locally while execution happens in the Linux subsystem.
